/**
 * @namespace {Object} gb
 */
var gb;
if (!gb)
	gb = {};
/**
 * @namespace {Object} gb.crs
 * @memberof gb
 */
if (!gb.crs)
	gb.crs = {};
/**
 * @namespace {Object} gb.edit
 * @memberof gb
 */
if (!gb.edit)
	gb.edit = {};
/**
 * @namespace {Object} gb.embed
 * @memberof gb
 */
if (!gb.embed)
	gb.embed = {};
/**
 * @namespace {Object} gb.footer
 * @memberof gb
 */
if (!gb.footer)
	gb.footer = {};
/**
 * @namespace {Object} gb.geoserver
 * @memberof gb
 */
if (!gb.geoserver)
	gb.geoserver = {};
/**
 * @namespace {Object} gb.header
 * @memberof gb
 */
if (!gb.header)
	gb.header = {};
/**
 * @namespace {Object} gb.interaction
 * @memberof gb
 */
if (!gb.interaction)
	gb.interaction = {};
/**
 * @namespace {Object} gb.layer
 * @memberof gb
 */
if (!gb.layer)
	gb.layer = {};
/**
 * @namespace {Object} gb.map
 * @memberof gb
 */
if (!gb.map)
	gb.map = {};
/**
 * @namespace {Object} gb.modal
 * @memberof gb
 */
if (!gb.modal)
	gb.modal = {};
/**
 * @namespace {Object} gb.module
 * @memberof gb
 */
if (!gb.module)
	gb.module = {};
/**
 * @namespace {Object} gb.overriding
 * @memberof gb
 */
if (!gb.overriding)
	gb.overriding = {};
/**
 * @namespace {Object} gb.panel
 * @memberof gb
 */
if (!gb.panel)
	gb.panel = {};
/**
 * @namespace {Object} gb.style
 * @memberof gb
 */
if (!gb.style)
	gb.style = {};
/**
 * @namespace {Object} gb.tree
 * @memberof gb
 */
if (!gb.tree)
	gb.tree = {};
/**
 * @namespace {Object} gb.validation
 * @memberof gb
 */
if (!gb.validation)
	gb.validation = {};
/**
 * @namespace {Object} gb.versioning
 * @memberof gb
 */
if (!gb.versioning)
	gb.versioning = {};
